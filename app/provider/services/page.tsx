import { PlaceholderPage } from "@/components/layout/PlaceholderPage";

export const metadata = { title: "Services" };

export default function ProviderServicesPage() {
  return (
    <PlaceholderPage
      eyebrow="Services"
      title="The work you offer."
      body="You’ll list the jobs your business can take from here. Editing services is a later phase — requests already flow through chat."
    />
  );
}
